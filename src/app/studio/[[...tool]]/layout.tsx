export const metadata = {
  title: 'DriveTwo Studio',
  description: 'Sanity Studio สำหรับ DriveTwo',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
